### Custom Base Image Build

#### Prerequisites

- Docker must be installed on your system.
- A valid `dockerfile.base` must exist in the repository.

#### Build the Custom Base Image

To build your custom base image from `dockerfile.base`, run the following command:

```bash
make build-base-image
```
